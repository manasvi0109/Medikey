# Add the remote repository (replace 'yourusername' with your GitHub username)
git remote add origin https://github.com/yourusername/medivault.git

# Stage all changes
git add .

# Commit the changes
git commit -m "Initial commit with complete MediVault application"

# Push to GitHub
git push -u origin main
